name: Test deployment

on:
  pull_request:
    paths-ignore:
      - '**/README.md'
    types: [opened, edited, synchronize, reopened]

jobs:
  test-deploy:
    uses: btat/rke2-product-docs/.github/workflows/reuse.yml@main
